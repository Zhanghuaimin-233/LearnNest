# 语栖 LearnNest 前端设计规范

- **文档用途：** 保证 WebUI 的视觉、布局、组件和交互表达一致
- **适用范围：** 普通用户可见的本地 WebUI
- **最后校准：** 2026-08-14

本文只描述前端页面应该呈现成什么样子，不定义产品路线、后端流程、数据结构、Provider 接口或安全实现。页面是否已经接入真实数据，以当前代码和运行结果为准。

## 1. 设计方向

语栖是一个安静、清楚的内容处理工作台。界面应让用户快速看清内容是否正在处理、是否需要操作，以及笔记和音频是否已经完成。

整体气质：

- 清爽，但不空泛；
- 克制，但不冷漠；
- 信息明确，不使用仪表盘式堆叠；
- 更像整理有序的桌面工作区，而不是后台管理系统。

视觉识别集中在一处：使用连续的“产出轨道”表达内容从进入到笔记、音频完成的过程。其余元素保持安静，不额外堆叠渐变、插画或装饰性卡片。

## 2. 页面骨架

### 2.1 一级导航

一级导航固定为：

```text
任务    来源    设置
```

- 桌面端位于左侧窄栏；
- 窄屏位于底部；
- 当前页面使用浅蓝底色和蓝色图标或文字；
- 不使用悬浮胶囊包裹整组导航。

### 2.2 页面层级

每个页面遵循同一结构：

```text
页面标题                         页面主操作
一句辅助说明（可选）
页面级标签或工具栏（可选）
────────────────────────────────────
页面主内容
```

页面标题、筛选和主内容属于同一个页面平面，不在主内容外再包一层“大卡片”。

### 2.3 页面就是主表面

禁止使用以下嵌套：

```text
子页面
└─ 大圆角页面卡
   ├─ 筛选卡
   └─ 内容卡
      └─ 详情卡
```

应改用留白、网格、标题层级、细分隔线和选中底色建立关系：

```text
页面标题与主操作
页面级筛选
────────────────────────────────────
列表                       详情
列表行                     信息区
列表行                     操作区
```

## 3. 视觉令牌

### 3.1 颜色

| 名称 | 色值 | 用途 |
| --- | --- | --- |
| Fog | `#F4F8FB` | 应用背景 |
| Paper | `#FFFFFF` | 输入框、弹层和必要的独立表面 |
| Ink | `#17212B` | 主文字 |
| Slate | `#607080` | 辅助文字 |
| Line | `#D8E2EA` | 分隔线和输入框边界 |
| Nest Blue | `#347FC4` | 主操作、当前项和键盘焦点 |
| Ice Blue | `#D9EAF7` | 选中背景和轻提示 |
| Pine | `#2E7D64` | 完成状态 |
| Ember | `#C45B4D` | 错误和需要处理 |
| Amber | `#A86F16` | 等待确认和警告 |

颜色使用规则：

- 蓝色只强调当前选择和主操作；
- 红色只表达错误或必须处理的事项；
- 状态不能只依赖颜色，必须同时提供文字或图标；
- 大面积背景不使用高饱和色或装饰性渐变。

### 3.2 字体

```css
--font-body: Inter, "Noto Sans SC", "Microsoft YaHei UI", sans-serif;
--font-data: "IBM Plex Mono", "Cascadia Mono", monospace;
```

- 页面标题：28px / 36px，字重 650；
- 区域标题：18px / 26px，字重 600；
- 正文：14px / 22px，字重 400；
- 辅助文字：13px / 20px，字重 400；
- 标签和按钮：13px / 20px，字重 550；
- 时间、数量和任务编号可以使用等宽字体。

不使用超大宣传标题。页面标题的任务是定位，不是制造海报效果。

### 3.3 间距

基础间距为 4px，常用序列：

```text
4 / 8 / 12 / 16 / 24 / 32 / 48
```

- 页面外边距：桌面端 32px，窄屏 16px；
- 标题到辅助说明：8px；
- 页面头到主内容：24px；
- 表单字段之间：20px；
- 列表行内边距：16px；
- 主要区域之间：32px。

### 3.4 圆角、边框与阴影

- 输入框、按钮：8px；
- 独立卡片：10px；
- 弹窗：14px；
- 胶囊状态标签：999px；
- 普通边框：1px `Line`；
- 页面内容不使用阴影；
- 只有弹窗、下拉菜单和临时浮层使用轻阴影。

### 3.5 动效

- 常规反馈：120–180ms；
- 面板进入：180–240ms；
- 只使用透明度和小幅位移；
- 处理进度可以有一次连续的轨道动效，其余位置保持静态；
- 遵守 `prefers-reduced-motion`。

## 4. 通用组件

### 4.1 按钮

按钮分为三级：

| 类型 | 用途 | 样式 |
| --- | --- | --- |
| 主按钮 | 当前页面唯一的主要动作 | 蓝底白字 |
| 次按钮 | 取消、返回、替代操作 | 白底深色字、细边框 |
| 文字按钮 | 行内操作和低频动作 | 无容器、蓝色文字 |

- 一个可视区域内通常只出现一个主按钮；
- 按钮文案直接描述结果，如“处理这个视频”“保存连接”；
- 危险操作不与主按钮共用蓝色；
- 加载时保留原宽度，避免布局跳动。

### 4.2 页面标签

“全部任务”“需要处理”“处理中”等页面标签直接放在页面标题下方：

- 使用文字、数量和底部选中线；
- 不放进卡片或胶囊组；
- 标签切换不移动页面标题和主操作；
- 数量为零时仍保持标签宽度稳定。

### 4.3 列表行

任务和连接优先使用列表行，不默认使用独立卡片：

- 行与行之间使用分隔线；
- 选中行使用 `Ice Blue` 背景和 3px 左侧蓝线；
- 标题、来源、时间和状态保持固定对齐；
- 行级操作在悬停、聚焦或选中后出现，但关键状态始终可见。

### 4.4 卡片

卡片只用于具有独立身份、能够单独选择或操作的对象，例如来源类型和可添加的模型服务。

以下内容不得使用卡片：

- 整个子页面；
- 页面筛选区；
- 页面标题和说明；
- 已经通过分栏或分隔线组织的任务详情；
- 仅用于增加背景色的普通说明文字。

### 4.5 状态标签

统一状态词：

```text
等待中    等待设置    等待确认    处理中    需要处理    已完成
```

- 标签使用浅色背景、深色文字；
- 同一状态在列表、详情和提示中使用同一名称；
- 不向普通用户显示内部阶段名、错误码或缩写。

### 4.6 表单

- 标签位于控件上方；
- 帮助文字紧随控件，不另建说明卡；
- 必填、错误和禁用状态位置固定；
- 保存后在原操作区反馈，不让用户寻找全局提示；
- 密钥输入默认遮蔽，并明确显示“已保存”或“未设置”。

### 4.7 空、载入与错误状态

- 空状态说明下一步能做什么，并提供一个直接操作；
- 骨架屏应模拟真实列表或表单结构；
- 错误信息说明发生了什么，以及用户可以采取的动作；
- 页面已有内容时，不用整页空状态覆盖它。

### 4.8 删除与危险操作

- 删除入口使用低强调的 Ember 文字按钮，不与页面主操作争夺注意力；
- 点击后必须打开确认弹窗，明确写出对象名称、会消失的内容和不会受影响的原始内容；
- 确认按钮沿用同一结果动词，如“移入回收区”，不使用含糊的“确定”；
- 正在执行、暂时不能删除时禁用入口，并在原位置说明原因；
- 取消后不改变当前选择、任务数量或页面滚动位置。

## 5. 任务页

### 5.1 桌面布局

```text
┌──────────────────────────────────────────────────────────┐
│ 任务                                  [处理单个视频]    │
│ 全部任务  需要处理  处理中  等待中  已完成              │
├──────────────────────┬───────────────────────────────────┤
│ 任务列表             │ 当前任务                          │
│                      │                                   │
│ 选中任务             │ 状态说明                          │
│ 其他任务             │ 内容—材料—笔记—音频产出轨道      │
│ 其他任务             │ 当前可用操作                      │
└──────────────────────┴───────────────────────────────────┘
```

- 列表和详情使用一条纵向分隔线；
- 页面摘要使用一句文字，不使用 KPI 卡片；
- 详情区域从状态结论开始，再展示产出轨道和操作；
- 笔记和音频完成后，在对应轨道终点提供“打开”操作。

### 5.2 产出轨道

产出轨道是语栖的主要识别元素：

```text
内容 ─── 材料 ─── 笔记 ─── 音频
 ●         ●         ◐         ○
```

- 每个节点包含名称和简短状态；
- 已完成使用绿色实心点；
- 当前处理中使用蓝色进度环；
- 等待使用空心点；
- 需要处理使用红色感叹号；
- 音频未选择时显示“未生成”，不表现为失败。

### 5.3 处理单个视频

“处理单个视频”始终是任务页右上角的主操作。点击后打开聚焦式弹窗或侧面板：

```text
处理单个视频

[ 选择本地视频 ]
或粘贴公开链接

产出
(●) 笔记
( ) 笔记和音频

预计会使用：笔记模型、播客模型、语音服务

[取消]                         [处理这个视频]
```

- 首屏只收集开始处理所需的信息；
- 缺少设置时在提交按钮附近提示，并提供“前往设置”；
- 提交成功后关闭弹层、选中新任务并回到任务详情；
- 不把这个流程拆成多张步骤卡。

## 6. 来源页

来源页按来源类型纵向排列：

```text
来源                                      [处理单个视频]

本地视频
从这台电脑选择一个视频
                                      [选择视频]
────────────────────────────────────────────
公开链接
[粘贴视频链接                         ] [添加]
────────────────────────────────────────────
抖音收藏
连接状态、最近同步时间                [同步收藏]
```

- 来源类型之间使用分隔线和留白；
- 简单来源不做成宣传式卡片；
- 只有需要选择的多个来源入口才使用紧凑选择卡；
- 添加后的处理状态统一在任务页查看。

## 7. 设置页

### 7.1 页面导航

设置页使用同一页面中的左侧锚点导航或顶部标签：

```text
产出与自动处理
模型与连接
调用安全
本地存储
```

每个分类直接占据页面区域，分类外不再套大卡片。

### 7.2 模型与连接

该区域需要同时让用户看见“现在在用什么”和“还能添加什么”：

```text
模型与连接

当前使用
笔记             MiMo 主连接                  [更换]
播客             DeepSeek 主连接              [更换]
语音             Windows 系统语音             [更换]

────────────────────────────────────────────

已保存连接                                      [添加连接]
MiMo 主连接     MiMo       已连接               [编辑]
DeepSeek 主连接 DeepSeek   已连接               [编辑]

────────────────────────────────────────────

可添加的服务
[ MiMo ]  [ DeepSeek ]  [ MiMo TTS ]  [ Windows 系统语音 ]
```

一致性规则：

- “当前使用”采用对齐列表，不使用卡片；
- “已保存连接”采用列表行；
- “可添加的服务”可以使用紧凑选择卡，因为每项是可单独选择的对象；
- 新出现的服务沿用同一选择卡和连接表单，不创建专属页面结构；
- 技术参数放在编辑表单中，列表只展示用户需要识别的信息。

### 7.3 设置反馈

- 修改过但未保存时，在分类标题旁显示“有未保存的更改”；
- 保存按钮位于当前分类末尾并保持可见；
- 保存中显示“保存中…”，成功后显示“已保存”；
- 页面刷新数据时，不改变正在编辑的输入内容；
- 连接错误出现在对应连接行或字段附近。

## 8. 文案规范

### 8.1 从用户视角命名

优先使用：

- “处理单个视频”，不用“创建 pipeline”；
- “笔记模型”，不用“note_writer binding”；
- “语音服务”，不用“TTS Provider”；
- “需要处理”，不用“needs_attention”；
- “打开笔记”，不用“查看 artifact”。

### 8.2 操作前后一致

按钮、进行中状态和成功提示使用同一动词：

```text
保存连接 → 保存中… → 已保存
同步收藏 → 正在同步… → 同步完成
处理这个视频 → 正在处理 → 已开始处理
```

### 8.3 错误文案

错误文案包含两部分：发生了什么，以及下一步怎么做。

```text
无法连接到 MiMo。请检查地址和密钥后重试。
音频生成失败。笔记已经完成，你可以单独重试音频。
```

不使用“未知错误”“请求失败”这类没有行动指引的孤立提示。

## 9. 响应式

### 9.1 桌面端

- 页面内容最大宽度 1440px；
- 任务页列表宽度约占 34%，详情占 66%；
- 设置页内容列最大宽度 880px，避免表单横向过宽。

### 9.2 窄屏

- 页面边距缩小为 16px；
- 一级导航移动到底部；
- 任务列表和详情变为上下结构；
- 表格改为带标签的纵向列表行；
- 弹窗在小屏使用接近全屏的底部面板；
- 主操作不能遮挡底部导航和最后一行内容。

## 10. 可访问性

- 所有可交互元素保留清晰的键盘焦点；
- 最小点击区域为 44 × 44px；
- 正文与背景对比度至少达到 WCAG AA；
- 图标按钮必须有可访问名称；
- 表单错误与对应字段建立程序关联；
- 动画不是理解状态的必要条件；
- 状态同时使用颜色、文字和形状表达。

## 11. 前端一致性检查

新增或修改页面时逐项确认：

- [ ] 页面标题、辅助说明和主操作位置与其他页面一致；
- [ ] 子页面没有再套一层整页卡片；
- [ ] 页面筛选没有放进卡片或胶囊容器；
- [ ] 列表、表单、卡片的使用场景符合本文规则；
- [ ] 颜色、字号、间距、圆角来自统一令牌；
- [ ] 同一状态在不同页面使用同一名称和颜色；
- [ ] 按钮文案说明点击后的实际结果；
- [ ] 空状态、加载状态、错误状态和保存反馈完整；
- [ ] 键盘焦点、触控尺寸和窄屏布局可用；
- [ ] 页面没有显示内部字段名、阶段名或错误码。

## 12. 维护规则

- 本文是前端一致性的基准，不记录后端实现细节和任务进度；
- 新组件先复用现有令牌和模式，确有新交互语义时才增加变体；
- 修改颜色、间距或核心组件时，应同步检查任务、来源和设置三个页面；
- 页面设计发生明确变更后更新本文，不在文档中保留多套并行方案；
- 原型只用于视觉确认，不代表真实数据接入或功能验收。
