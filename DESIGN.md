# 语栖 LearnNest 前端设计规范

- **文档用途：** 统一普通用户 WebUI 的信息架构、视觉令牌、组件、文案与响应式表达
- **适用范围：** 任务工作台、任务详情、来源、设置、成品阅读和事务弹窗
- **最后校准：** 2026-09-01（W3.1 模型目录与显式模型选择）
- **当前基准：** 用户确认的高密度任务工作台、跨页面延展与设置页模型选择交互

本文只定义页面如何组织和表达真实产品能力。产品范围、Provider、任务事实、安全和恢复合同以代码、`AGENTS.md` 与私有资料仓现役 Plan/State 为准；视觉完成不等于真实 Provider 或人工成品验收。

## 1. 设计命题

语栖是个人在本机使用的学习内容产出控制台。它不是宣传网站，也不是企业后台模板。页面首先帮助用户回答四件事：

1. 什么内容已经进入语栖；
2. 现在处理到哪里；
3. 哪件事需要自己操作；
4. 笔记或音频是否已经可以使用。

整体特征：

- **高密度但不拥挤：** 同一屏保留更多真实任务、来源或设置事实，减少超大标题和无效留白；
- **三栏分工：** 左侧负责范围与分类，中间承载主要事实，右侧解释优先事项、状态和下一步；
- **细线组织：** 主要依靠网格、表头、1px 分隔线和克制的选中底色建立层级；
- **单一强调蓝：** 蓝色只表示当前选择、主要动作和处理中事实，状态色不承担装饰；
- **人话优先：** 默认显示发生了什么和下一步，技术事实只在确有诊断价值时展开；
- **真实能力优先：** 不为复刻参考图而虚构批量操作、日志、网络状态、工具版本或不存在的页面。

语栖的识别元素是 **环形进度 + 细产出轨道**。它们共同表达“内容进入 → 材料准备 → 笔记 → 音频”，其余区域保持安静。

## 2. 视觉令牌

### 2.1 颜色

| 名称 | 色值 | 用途 |
| --- | --- | --- |
| Fog | `#F6F8FB` | 应用背景与低强调输入底色 |
| Paper | `#FFFFFF` | 主事实表面、独立对象和弹窗 |
| Ink | `#172033` | 标题和主要文字 |
| Slate | `#66758A` | 说明、元信息和次要文字 |
| Muted | `#8996A8` | 表头、计数和弱提示 |
| Line | `#E5EAF1` | 分隔线与普通边框 |
| Nest Blue | `#1769FF` | 当前项、主操作、焦点和处理进度 |
| Ice Blue | `#E8F1FF` | 选中背景和蓝色浅标签 |
| Pine | `#20AD69` | 已完成、可阅读和可播放 |
| Amber | `#A86F16` | 已暂停、等待确认和许可变化 |
| Ember | `#C45B4D` | 失败、危险操作和必须处理 |

规则：

- 状态必须同时有文字或形状，不能只依赖颜色；
- 红色只用于错误、阻塞和危险动作；
- 不使用装饰性渐变、大面积高饱和背景或彩色 KPI 卡片；
- 主内容阴影保持极轻，只用于把事实表面从 Fog 背景中分离。

### 2.2 字体角色

```css
--body-font: Inter, "Noto Sans SC", "Microsoft YaHei UI", "Segoe UI", sans-serif;
--display-font: "Noto Sans SC", "Microsoft YaHei UI", "Segoe UI", sans-serif;
--brand-font: "STKaiti", "KaiTi", "Microsoft YaHei UI", sans-serif;
--data-font: "IBM Plex Mono", "Cascadia Mono", monospace;
```

- 楷体只用于“语栖”品牌，不再用于页面标题；
- 页面标题使用 30px 左右的无衬线粗体，负责定位而不是制造海报；
- 区域标题 18–21px，列表标题 13–15px，正文 12–14px；
- 任务 ID、百分比、数量和时间可以使用等宽字体；
- 表头、辅助标签和数据说明使用 9–11px，但不得承载关键操作结论。

### 2.3 间距、边框和圆角

- 基础间距序列：`4 / 8 / 12 / 16 / 20 / 24 / 32`；
- 桌面页边距 34px，窄屏 12–16px；
- 三栏间距 20px；
- 主事实表面和侧栏圆角 14px，输入与按钮 9–10px，状态标签为胶囊；
- 普通边框使用 1px `Line`；当前侧栏项增加 3px 左侧蓝线；
- 表格行高通常为 92–112px，侧栏项高 40–50px，点击目标不小于 36px，关键触控目标不小于 44px。

## 3. 全局页面骨架

### 3.1 一级导航

```text
任务    来源    设置
```

- 桌面端位于 76px 顶栏，与品牌和付费整理许可状态同层；
- 窄屏移动到底部，保持三个入口，不重复“今日”“所有任务”或“学习库”假入口；
- 当前项使用浅色背景、蓝色文字和底部蓝线；
- 顶栏状态只解释当前许可及其影响，不展示本地路径、SHA、secret 或内部 stage。

### 3.2 桌面工作区

任务、来源和设置共享最高 2048px 的流式工作区：

```text
┌──────────────┬──────────────────────────────────┬──────────────────┐
│ 范围 / 分类  │ 主事实表面                       │ 态势 / 下一步    │
│ 约 232px     │ 使用剩余空间                     │ 约 300px         │
└──────────────┴──────────────────────────────────┴──────────────────┘
```

- 左栏回答“现在看哪一类”；
- 中栏回答“真实内容和设置是什么”；
- 右栏回答“当前最值得关注什么、下一步是什么”；
- 页面标题保持紧凑，不用超大 Hero 把三栏推到首屏以下；
- 只有独立事实表面使用白底和边框，不把整个子页面再包进一层大卡片。

三栏是一套职责分工，不要求所有页面机械同构。成品阅读以连续阅读为首要任务，因此使用阅读索引 + 正文双栏。

### 3.3 中等宽度与窄屏

- 1400px 以下：右侧态势栏移到主事实表面下方，三张状态卡横向排列；
- 760px 以下：一级导航固定到底部，页面按“分类 → 最重要状态 → 主内容”纵向排列；
- 表格表头隐藏，行改成带标签的纵向事实块；
- 来源类型和设置分类变成横向或紧凑标签，不产生横向页面溢出；
- 窄屏详情隐藏总览态势卡，只显示当前任务事实和动作；
- 弹窗成为接近全屏的底部面板，不能遮挡底部导航或最后一项内容。

## 4. 通用组件

### 4.1 事实表面与卡片

白色事实表面用于：

- 任务表、来源快速添加表、收藏浏览器、设置主面板；
- 任务的当前动作、连接状态和明确的独立对象；
- 弹窗和阅读正文。

卡片不得用于：

- 仅增加背景色的说明；
- 页面标题、筛选或整个子页面；
- 可以用一行表格或分隔线清楚表达的普通事实；
- 虚构统计、日志或系统健康项。

### 4.2 列表与表格

- 表头固定列语义，行内信息保持对齐；
- 标题在前，来源、ID、时间等元信息降低强调；
- 当前行使用浅蓝底和左侧蓝线；
- 主要动作位于最右列，关键状态始终可见；
- 行在不同页面可改变列数，但“对象 → 状态 → 说明 → 操作”的阅读顺序不变。

### 4.3 状态与进度

普通用户状态统一使用：

```text
等待中    等待设置    等待付费许可    处理中
已暂停    处理已停止  可阅读          可播放
```

- 任务列表使用环形百分比 + 3px 进度线；
- 任务详情使用产出轨道；
- 完成是绿色，处理中是蓝色，等待/暂停是琥珀色，失败是红色；
- 不显示内部状态名、异常类型、SHA、attempt 或 Provider 原始响应。

### 4.4 按钮

| 类型 | 用途 | 表达 |
| --- | --- | --- |
| 主按钮 | 当前区域唯一的推进动作 | 蓝底白字 |
| 次按钮 | 同级替代、取消、刷新 | 白底细边框 |
| 文字按钮 | 低频跳转、行内操作 | 无容器蓝字 |
| 危险按钮 | 移入回收区等不可忽略后果 | 红色且需要确认 |

- 文案直接描述结果，如“添加并处理”“保存调用限制”；
- 加载时保持按钮宽度，显示“保存中…”等原位反馈；
- 危险操作不与主按钮共用蓝色，也不使用含糊的“确定”。

### 4.5 表单与反馈

- 标签在控件上方，帮助文字紧邻字段；
- 保存反馈回到当前表单或设置分类，不要求等待轮询；
- focused 或 dirty 表单不被后台轮询覆盖；
- 错误同时说明发生了什么和下一步；
- 密钥默认遮蔽，只显示“已保存/未设置”，绝不回显明文。

### 4.6 空、载入和失败

- 空状态给出一个真实下一步，例如“从来源添加内容”；
- 搜索无结果应明确说明清除搜索或切换筛选；
- 页面已有内容时，局部失败不能覆盖整页；
- 失败原因放在发生问题的对象附近，并保留已经完成的内容。

## 5. 页面蓝图

### 5.1 任务工作台

```text
任务状态筛选 │ 高密度任务表                       │ 优先任务
             │ 任务 / 进度 / 成品 / 说明 / 操作  │ 最近活动
             │                                    │ 快速前往
             │                                    │ 运行状态
```

- 默认首页按“需要处理 → 已暂停 → 处理中 → 等待中 → 已完成”表达优先级；
- 左栏只做状态筛选和计数，回收站独立于活跃任务；
- 中央表支持标题、来源和状态搜索；
- 右栏只投影真实任务顺序、默认成品、付费许可、新收藏与检查间隔；
- 新增内容只从来源页进入，任务页不复制上传入口。

### 5.2 任务详情

进入详情后仍保留任务筛选栏，但隐藏总览态势栏，让详情占据中栏和右栏：

```text
任务筛选 │ 当前任务事实与产出轨道              │ 当前状态与唯一主操作
```

- 标题、来源和人话状态先出现；
- 产出轨道只渲染当前目标所需节点；
- 右侧动作卡说明“当前发生什么、下一步是什么”；
- 暂停只阻止后续调度，不能暗示已停止正在运行的 Provider 或材料子进程；
- 删除只移入回收区，运行中或仍被认领时必须拒绝并说明原因。

### 5.3 来源

```text
来源类型 │ 快速添加：本地视频 / 公开链接        │ 抖音连接
         │ 抖音收藏夹与作品                     │ 加入路径
         │ 最近来源处理                         │ 添加后去哪里
```

- 本地视频和公开链接使用紧凑表格行，不做宣传式大区块；
- 抖音收藏作为真实内容库，可使用目录栏与紧凑封面卡；
- 连接状态、最近同步和安全说明集中在右栏；
- 登录验证、同步、收藏选择和来源任务使用原位状态，不让轮询覆盖本轮失败；
- 添加成功后回到任务工作台，三类来源不复制后续生产操作。

### 5.4 设置

```text
设置分类 │ 当前分类表单与事实列表               │ 准备情况
         │                                      │ 当前设置摘要
         │                                      │ 保存 → 授权 → 添加
```

- 左栏固定为“产出与自动处理 / 模型与连接 / 调用安全 / 本地存储”；
- 中栏一次只显示一个分类，表单与连接使用行和分隔线；
- 右栏持续显示默认成品、付费许可、新收藏和 30 分钟检查间隔；
- “保存设置 → 明确授权 → 添加内容”是有业务顺序的三步提示；
- 付费整理许可与自动加入新收藏是两个独立状态；任何实质设置变化使旧许可失效；
- 本地存储直接显示真实路径，但页面、日志和普通任务状态不得把路径发送给 Provider。

LLM 连接的模型选择遵守以下交互：

- 已保存连接只在用户点击“获取模型”后读取目录；页面加载、轮询和打开弹窗不得自动访问 Provider；
- 弹窗先显示当前模型，目录请求提供加载、空、错误和搜索状态；不重复显示已由连接卡表达的连接名
  或 Provider 名称；
- 获取结果只形成候选，不自动切换模型；用户必须选中具体模型并显式保存；
- 保存成功立即使用 API 返回事实更新连接卡，并说明受影响职责及重新确认付费许可的需要；
- 连接设置变化、弹窗关闭或新请求开始后，旧目录失效；迟到响应不得覆盖当前连接或重新打开状态；
- 页面只展示 LearnNest 已适配的模型，不显示原始 Provider 响应、密钥、目录 URL 或内部兼容判断。

### 5.5 成品阅读

阅读页不显示控制台三栏，而使用：

```text
阅读索引 │ 笔记标题
         │ 配套音频（通过安全验证时）
         │ 连续正文、图片、表格与代码
```

- 顶栏保留语栖品牌和明确的“返回任务工作台”；
- 左栏只显示成品类型、笔记状态和音频状态；
- 正文行宽、标题层级、表格、代码、引用和安全图片统一排版；
- 音频只有通过所有权、路径、SHA 与可解码性检查后才显示；
- 不保留当前不存在的“学习库”返回入口。

### 5.6 事务弹窗

单视频、连接、付费许可和删除使用同一种弹窗骨架：标题与后果 → 必要事实 → 取消/确认。

- 取消为默认安全动作；
- 付费确认明确列出默认成品、适用范围和费用边界；
- 删除明确说明会移入回收区、不会删除原始视频；
- 窄屏改为底部面板，关闭后保持原选择和滚动位置。

## 6. 文案与可访问性

- 从用户一侧命名：“笔记模型”“语音服务”“需要处理”“打开笔记”；
- 操作前后一致：`保存连接 → 保存中… → 已保存`；
- 不使用孤立的“未知错误”“请求失败”，必须给出可执行下一步；
- 所有图标按钮有可访问名称，所有交互保留清晰焦点；
- 正文对比度达到 WCAG AA；状态同时使用颜色、文字和形状；
- 尊重 `prefers-reduced-motion`，动画不是理解状态的必要条件。

## 7. 静态资源与升级一致性

- HTML、CSS 和 JavaScript 必须作为同一界面版本交付，不能让新版 DOM 与旧样式混用；
- 工作台和阅读页引用同一个显式资源版本；修改 CSS/JS 时同步更新版本值；
- `/` 与 `/static/*` 响应使用 `Cache-Control: no-cache`，已有浏览器标签页普通刷新即可重新验证；
- 强制刷新和 cache-buster 只能用于诊断，不能替代真实用户升级路径验收；
- UI 改版必须同时覆盖全新浏览器与已有标签页刷新。

## 8. 一致性检查

新增或修改页面时逐项确认：

- [ ] 页面单一任务明确，左/中/右职责没有重复；
- [ ] 页面标题紧凑，主要事实在首屏可见；
- [ ] 没有为填满版面虚构功能、状态或统计；
- [ ] 表格、列表、卡片和弹窗的使用符合本文职责；
- [ ] 颜色、字号、间距、圆角来自统一令牌；
- [ ] 同一状态在任务、来源和设置中使用同一词语与颜色；
- [ ] 空、加载、失败、保存和 dirty 状态完整；
- [ ] 桌面、中等宽度、390px 窄屏均无横向溢出；
- [ ] 新版 HTML 不会与旧 CSS/JS 缓存混用；
- [ ] 页面没有泄露本地路径、secret、SHA、内部 stage 或原始 Provider 内容。

## 9. 维护规则

- 本文只保留已经选定并实际采用的现役设计，不保留并行候选；
- 设计变化先在真实页面实现并进行桌面/窄屏检查，再回校本文；
- 修改公共令牌、导航、事实表面或资源版本时，回归任务、详情、来源、设置和阅读页；
- 机器测试、真实 Edge、用户体验、真实 Provider 和人工成品验收分别记录，不能互相替代。
